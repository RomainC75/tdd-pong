import { describe, it, expect } from "vitest";

type TPosition = {
  x: number,
  y: number
}

describe("loop test", () => {
  it("should move the snake", ()=>{
    const snake: TPosition = [
      []
    ]
  });
});
